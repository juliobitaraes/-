/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],  // Empty content = include ALL Tailwind classes (production-ready complete build)
  theme: {
    extend: {},
  },
  plugins: [],
  darkMode: 'class',
}
